
repos:
# Since we are tracking .py file, we do not have to clear output of Jupyter Notebooks.
# However, if you want to clear output of Jupyter Notebooks, uncomment the following section
#   - repo: local
#     hooks:
#       - id: jupyter-nb-clear-output
#         name: jupyter-nb-clear-output
#         files: \.ipynb$
#         stages: [pre-commit]
#         language: system
#         entry: |
#           jupyter nbconvert
#           --inplace
#           --ClearOutputPreprocessor.enabled=True
#           --ClearMetadataPreprocessor.enabled=True

  # - repo: https://github.com/astral-sh/ruff-pre-commit
  #   # Ruff version.
  #   rev: v0.9.10
  #   hooks:
  #     # Run the linter.
  #     - id: ruff
  #       args: [--fix]
  #     # Run the formatter.
  #     - id: ruff-format
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: check-yaml
  # Generate notebooks from Python scripts - skipped in pre-commit for now
  # Run 'python make_notebooks.py' manually after changes to notebook_scripts/
